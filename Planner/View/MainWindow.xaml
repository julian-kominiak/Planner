<Window x:Class="Planner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Planner"
        xmlns:qv="clr-namespace:Planner.View"
        xmlns:vm="clr-namespace:Planner.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" FontFamily="Lucida Sans"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None">

    
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Border CornerRadius="35" BorderThickness="20" BorderBrush="#FF535353">
        <Grid Background="#FF535353">
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="95*"/>
            </Grid.RowDefinitions>
            <Button Content="X" Width="20" HorizontalAlignment="Right" Foreground="White" BorderBrush="Transparent" Background="Transparent"/>
            <qv:PlannerView
        Grid.Row="1"
        SelectedDate="{Binding Path=Planner.SelectedDate, Mode=TwoWay}"
        Label="{Binding Path=Planner.Label, Mode=TwoWay}"
        />
        </Grid>
    </Border>
    
    
</Window>
